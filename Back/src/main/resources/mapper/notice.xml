<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.model.mapper.NoticeMapper">
	
	<resultMap type="NoticeDto" id="noticeDto">
		<result property="articleno" column="articleno"/>
		<result property="subject" column="subject"/>
		<result property="userid" column="userid"/>
		<result property="regtime" column="regtime"/>
		<result property="viewcnt" column="viewcnt"/>
		<result property="content" column="content"/>
	</resultMap>

	<select id="selectNotice" resultMap="noticeDto">
		select * from notice
		order by articleNo desc
	</select>

	<insert id="insertNotice" parameterType="NoticeDto">
		insert into notice (subject, userid, viewCnt, content)
		values (#{subject},#{userid},#{viewcnt},#{content})
	</insert>
	
	<update id="updateNotice" parameterType="NoticeDto">
		update notice
		set subject = #{subject}, content = #{content} where articleNo = #{articleno}
	</update>
	
	<delete id="deleteNotice" parameterType="int">
		delete from notice
		where articleno = #{no}
	</delete>

	<select id="searchNotice" parameterType="int" resultType="NoticeDto">
		select * from notice
		where articleNo = #{articleNo}
	</select>
	
	<update id="increaseViewCnt" parameterType="int">
		update notice
		set viewCnt = viewCnt + 1
		where articleNo = #{articleNo}
	</update>

	
</mapper>